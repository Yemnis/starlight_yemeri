# ============================================
# REQUIRED CREDENTIALS
# ============================================

# OpenAI API Key (for Whisper audio transcription)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-key-here

# GCP Project ID (also used as x-goog-project-id for GCS)
# Example: gen-lang-client-0082081331
# Find in GCP Console dashboard
GCP_PROJECT_ID=your-gcp-project-id

# Cloud Storage Bucket (for storing videos, audio, scenes, thumbnails)
# Create in: https://console.cloud.google.com/storage
# Format: just the bucket name (e.g., "my-project-videos")
# Note: Ensure your service account has "Storage Object Admin" role
GCS_BUCKET=your-bucket-name

# ============================================
# SERVICE ACCOUNT (Already Configured)
# ============================================

# Path to your service account JSON key file
GOOGLE_APPLICATION_CREDENTIALS=./config/service-account-key.json
GCP_SERVICE_ACCOUNT_KEY=./config/service-account-key.json

# ============================================
# SERVER CONFIGURATION
# ============================================

PORT=3001
NODE_ENV=development

# ============================================
# GCP CONFIGURATION
# ============================================

# GCP Region/Location
GCP_LOCATION=us-central1
VERTEX_AI_LOCATION=us-central1

# ============================================
# VERTEX AI MODELS (Optional - have defaults)
# ============================================

# Model for scene analysis (default: gemini-2.5-flash)
GEMINI_NANO_BANANA_MODEL=gemini-2.5-flash

# Model for chat/Q&A (default: gemini-1.5-pro)
GEMINI_PRO_MODEL=gemini-1.5-pro

# Embedding model for vector search (default: text-embedding-004)
EMBEDDING_MODEL=text-embedding-004

# ============================================
# CORS CONFIGURATION
# ============================================

FRONTEND_URL=http://localhost:5173

# ============================================
# OPTIONAL: ADVANCED SETTINGS
# ============================================

# Firestore database (default: (default))
FIRESTORE_DATABASE=(default)

# Vector Search (leave empty if not using vector search yet)
VECTOR_INDEX_ID=
VECTOR_INDEX_ENDPOINT=

# Video processing limits
MAX_VIDEO_SIZE_MB=500
MAX_SCENES_PER_VIDEO=20
SCENE_DETECTION_THRESHOLD=0.4

# Storage settings
SIGNED_URL_EXPIRY_HOURS=1

# FFmpeg settings
FFMPEG_AUDIO_BITRATE=128k
FFMPEG_SAMPLE_RATE=16000
FFMPEG_THUMBNAIL_SIZE=1024x1024
